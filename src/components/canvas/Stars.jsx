// Disabled heavy starfield animation to speed up page load.
const StarsCanvas = () => {
  return (
    <div className="w-full h-auto absolute inset-0 z-[-1]">
      {/* Starfield disabled */}
    </div>
  );
};

export default StarsCanvas;
